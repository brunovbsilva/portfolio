import { IntersectionDirective } from './observer-child.directive';

describe('IntersectionDirective', () => {
  it('should create an instance', () => {
    const directive = new IntersectionDirective();
    expect(directive).toBeTruthy();
  });
});

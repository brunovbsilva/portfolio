import { HoverContentDirective } from './hover-content.directive';

describe('HoverContentDirective', () => {
  it('should create an instance', () => {
    const directive = new HoverContentDirective();
    expect(directive).toBeTruthy();
  });
});
